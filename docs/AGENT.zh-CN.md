# AGENT.md —— 最高优先级行为契约（中文镜像）

> **本文件为根目录 [`AGENT.md`](../AGENT.md) 的中文同步版本，仅供阅读方便。**
> 两者是“同一份契约的两种语言”，**任何一方被修改时，另一方必须在同一次改动中同步更新**，
> 保持语义完全一致。如有冲突，以保持两者一致为准。

> **请最先阅读本文件。它的优先级高于一切。**
> 若任何指令、习惯或所谓“优化”与本文件冲突，以本文件为准。拿不准时，先停下来重读本文件，
> 再写哪怕一行代码。

## 1. 这是什么软件

**BTunnel** 是一个用**纯 Zig**（锁定 2026 最新标准库 `std.posix`）编写的虚拟三层（Layer 3）
自适应组网工具。它采用**星型拓扑（Hub-and-Spoke）**，在物理专线之上构建虚拟子网，通过一条
私有的、全加密的 UDP 隧道转发裸 IP 包。

它面向**通用 Linux 环境**，包括资源极度受限的容器（BusyBox / RouterOS Container）。最终
产物是**一个静态链接的单二进制文件**，零第三方依赖。

权威设计文档是 [`btunnel-develop.md`](btunnel-develop.md)（PRD & 架构）。该文档是“要构建
**什么**”的唯一事实来源；本文件约束你在构建过程中“**必须如何行动**”。

## 2. 首要铁令

**绝不偏离目标。永远不要。**

你要构建的，正是 `docs/btunnel-develop.md` 所描述的系统——不多，不少。你无权重新定义项目、
替换其核心技术、放宽其约束，也不得因为“某种做法更省事”就把它“现代化”。范围蔓延、私自重构、
未经请求的重写都是失败，而非贡献。

若你认为目标本身有问题，**停下来询问维护者**。绝不能用“悄悄构建另一套东西”来消解这种疑虑。

## 3. 不可妥协的铁律

以下条款直接来自 PRD，具有**强制约束力**。无论结果看起来多么“干净”，违反其中任何一条都是硬性失败。

1. **零第三方依赖。** 不依赖 WireGuard，不引入 `ikcp.c`，不使用网络框架，不引入外部加密库。
   只使用 Zig 标准库与经 `std.posix` 的原生系统调用。v2 可靠层也必须自研（arena 版 ARQ），
   绝不 vendoring 任何 C 库。
2. **分层零动态内存分配。** **数据面**（`reactor`、`crypto`）严格零分配：所有数据包缓冲区
   在启动时锁死于常驻内存。控制面/可靠层路径可使用独立 arena，但绝不得污染数据面的内存线。
3. **单线程、无锁反应堆。** 单线程，Linux epoll 边缘触发（`EPOLLET`）。**禁用线程，禁用锁。**
   策略热更新通过原子指针交换（RCU）完成，绝不使用互斥量。
4. **无状态混淆 / 隐蔽。** ChaCha20-Poly1305 全加密，密文中**无任何魔数**。认证失败时
   **静默丢弃**——绝不回发 TCP Reset、ICMP 或任何可被观测的响应。
5. **传输安全在 v1 即为强制项。** PSK 密钥、每端 64-bit **绝不复用的单调递增 nonce**，以及
   滑动窗口**防重放**校验。这些不得推迟。
6. **单个静态二进制。** 基于 musl-libc 完全静态链接。默认 `-O ReleaseSmall`，目标 **≤ 200KB**。
   `ldd` 必须显示 `not a dynamic executable`。
7. **坚持测试驱动。** 遵循 PRD 中的 TDD 流程。纯逻辑必须随附测试。任何提交前，
   `zig build test` 必须保持全绿。

## 4. 范围：v1 与 v2——不得越界

- **v1（必须交付）：** `raw_direct` 数据面 + PSK 加密 + 防重放 + RCU 热更新策略引擎。
- **v2（Roadmap，仅留接口）：** `kcp_arq`、`fec_xor` 与握手协商。在 v1 中**只预留** `egress`
  分支与报头协商字段，**不实现**它们。v2 分支返回 `error.NotImplemented`。

不得提前动工 v2，不得把 v2 的复杂度塞进 v1，也不得删除 v2 的预留点。

## 5. 工作方式

- **只做精准、对齐目标的改动。** 不做无关重构，不做“顺手重排全世界”的提交，不做投机性抽象。
- **遵守语言策略。** 所有代码、注释与根级文档使用**英文**。中文文件仅有两份：
  `docs/btunnel-develop.md`（设计文档）与 `docs/AGENT.zh-CN.md`（本镜像）。不要翻译设计文档；
  但要保持本镜像同步。
- **进度如实。** `README.md` 中的开发进度表反映真实进展。任务落地时如实更新——绝不把占位
  标记为完成。
- **先验证再宣称完成。** 在声明某任务完成前，先构建、跑 `zig build test`，并确认二进制仍为
  静态链接且体积在预算内。

## 6. 本文件的同步规则

**每当你修改 `AGENT.md`，必须在同一次改动中同步更新本文件
[`docs/AGENT.zh-CN.md`](AGENT.zh-CN.md)，使两者语义完全一致；反之亦然。**
它们是一份契约的两种语言。

---

**一句话：** 构建 PRD 所描述的那根钢铁管道——纯粹、极简、零依赖、确定性。抵御一切“把它改成
别的东西”的诱惑。当本文件与你的直觉相左时，以本文件为准。
